# Gradle / build
build/
.gradle/
run/
out/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Local
logs/
*.log

# Local development dependency jars.
# libs/ holds the dev jars build.gradle reaches for (NEO ECO, AECS, ModDevMCP). Each one is a build
# artifact of its own repository, copied in by hand; the comments beside each dependency say which.
libs/*.jar

# Pi workflow metadata
plans/
.pi/
