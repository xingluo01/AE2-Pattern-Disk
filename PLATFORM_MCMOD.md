[h1=概述]

简单说：这是一个应用能源 2（AE2）的扩展模组，为我的世界增加了高容量样板存储、ME样板磁盘供应器、ME样板转存器、ME样板磁盘编码终端，以及两种更强的分子装配室。

把编码好的样板放进磁盘，而不是塞满供应器的槽位；磁盘可以随身携带、按类型标记，并在编码终端里集中管理。

[h2=模组特点]

[h3=样板磁盘]

把编码好的 AE2 样板存进单个磁盘。空磁盘没有类型，第一张放入的样板决定它存储哪种样板；一个磁盘同时只接受一种样板类型。

五个等级：1k 存 4 张、4k 存 16 张、16k 存 64 张、64k 存 256 张、256k 存 1024 张。

磁盘还可以带一个「标记」，记录它属于哪台机器或哪种配方类型。标记会显示在编码终端的磁盘列表里（悬停磁盘可看到「标记：」一行），不会改写磁盘名称，也是该列表默认的筛选依据。

[h3=样板磁盘供应器]

一个由样板磁盘驱动的样板供应器。把磁盘放进槽位后，它会把磁盘中的编码样板暴露给 ME 自动合成系统。它是任务来源，不自己合成，并通过返回库存接收产物。

它有两种形态，共用同一套实现：方块形态，以及像 AE2 原版线缆样板供应器那样贴在 ME 线上的面板形态。两种形态都是 9 个磁盘槽、同一份返回库存，面板形态只是占地更薄。

装了 ExtendedAE Plus 时，它的「上传样板到供应器」按钮在这里同样可用：供应器自己没有样板槽，上传的样板会落到某张磁盘的空位里。

[h3=样板转存器]

在 AE2 样板和样板磁盘之间移动编码样板。输入槽可放编码样板或已填充磁盘，目标槽放目标磁盘。提取出的空白样板会返回连接的 ME 网络，支持加速卡。

[h3=样板磁盘编码终端]

挂在 ME 线上的面板，把「编码样板」这件事和样板磁盘接到一起。它会扫描整个 ME 网络上的样板磁盘；选一张磁盘，就能把刚编码好的样板写进去，或者查看和整理它已经存下的样板。

支持工作台、处理、锻造台、切石机四种编码模式，用模式按钮切换；处理模式额外提供副产物轮换、乘除（点一下 ×2，Shift ×3，Ctrl ×5，Alt ×10）和同物品合并。

空白样板直接取自 ME 网络：槽位只读显示网络存量，不需要手动放。

鼠标操作：左键把当前编码的样板写进磁盘；拿着工作方块右键可覆盖磁盘标记；Shift + 右键把搜索框里的文字写成标记，搜索框为空则清除标记；拿着工作方块中键点击，可按标记对应的机器给磁盘改名。

每次写入都会在聊天栏报告结果：写到了哪张磁盘，或者为什么被拒绝（磁盘已满、锁定了别的样板类型、已有同产物配方、样板类型无法识别等）。列表里不是恰好一张磁盘时，样板会留在编码槽等你确认；搜索后只剩一张时，点编码会直接写进那张盘。

[h3=高效分子装配室]

并行分子装配室，拥有 8 个独立执行线程。它接受 AE2 样板供应器推送的合成任务并并发执行。每个线程有独立的 3×3 分子装配室网格、输出槽和进度。

GUI 每线程一页，仅当前页显示垂直进度。

最多安装 5 张 AE2 加速卡，倍率为 1.0x / 1.3x / 1.7x / 2.0x / 2.5x / 5.0x。

每页可选样板槽。放入编码合成样板后，该页变成自执行单元：自己从 ME 网络拉取材料，材料足够时持续合成，并把产物和容器剩余物推入相邻库存或网络。

正在运行手动样板的页面不接受供应器推送任务，因此机器只在至少一页空闲时接受推送任务。

[h3=批处理装配室]

把 AE2 合成 CPU 推送的任务缓存到 9 个私有存储元件槽中，等输入材料真正停止到达后再执行。

批处理窗口：标准 40 Tick，快速 10 Tick；材料一直在来，批次就继续等。除了插入元件的容量，缓冲本身不额外设上限，已开始的批次会一直跑到队列结束。

支持工作台、锻造台、切石机配方。

输出会平滑返回网络，约 20 Tick 内按每 Tick 约 5% 回流；网络暂时收不下时会留在队列重试，不会因此回滚输入。

只有元件放不下推送、材料缺失或电网断电时，输入才会回滚。

存储元件为机器私有，不会暴露给 ME 网络；工作时锁定。破坏方块或按取消会把缓冲返回网络。机器空闲 10 Tick 后，元件里剩下的东西也会返回网络。

队列只存在内存里：重新载入后队列清空，材料按上面的空闲回流返回网络；正在等这批任务的合成 CPU 任务需要手动取消。

最多 4 张加速卡，增加 2 / 4 / 8 / 16 个分析线程；但元件和网络访问仍在服务器线程执行，因为 AE2 存储不是线程安全的。

总输出始终匹配有序数量。

[h2=配方与指南]

1k 样板磁盘：空白样板 + 物品元件外壳（无序合成）；也有 AE2 风格的 3×3 合成。

4k / 16k / 64k / 256k：由低阶磁盘 + 计算处理器 + 石英玻璃等升级而来（16k、64k 用荧石粉，256k 用天空粉尘）。

样板磁盘供应器：样板供应器 + 容量卡（无序合成）；方块形态与面板形态可 1:1 互转，不需要额外材料。

样板转存器：石英玻璃 + 1k 样板磁盘 + 物品元件外壳 + 逻辑处理器 + 工程处理器。

高效分子装配室：8 个分子装配室围 1 张容量卡。

批处理装配室：石英玻璃 + 计算处理器 + 高效分子装配室 + 64k 样板磁盘。

样板磁盘编码终端：样板编码终端 + 1k 样板磁盘（无序合成）。

[h3=配方转移（EMI / JEI）]

装了 EMI 或 JEI 时，在编码终端界面里可以用配方页的配方转移按钮（EMI 的 +、JEI 的配方转移）把配方直接填进编码网格。

装了 EMI 或 JEI 才有这个功能，两者都不装时模组其余功能不受影响。EMI 还会把磁盘标记里的配方类别名显示为本地化名称。

[h3=内置指南]

模组内置 GuideME 指南，覆盖样板磁盘（五级合在一页）、样板磁盘供应器（两种形态）、样板转存器、高效分子装配室、批处理装配室和样板磁盘编码终端，支持英文与简体中文。

5 个机器 GUI 里，目前只有编码终端能从界面直接跳到对应指南页。

[h2=依赖与许可]

必需：NeoForge、Minecraft 1.21.1、AE2 19 或更高。

GuideME：内置指南需要它。

可选：NEO ECO AE Extension（需要带样板磁盘集成钩子的版本，编码终端才会多一个上传到计算集群的按钮）、ExtendedAE Plus（供应器可用「上传样板到供应器」按钮）。

许可证：LGPL-3.0。

部分纹理和模型来自 AE2，同样按 LGPL-3.0 分发；批处理装配室的方块纹理与发光外壳模型为作者自有素材（ARR），不在 LGPL-3.0 授权范围内。

EMI / JEI 集成为本模组自己的实现。

[h1=画廊]

