# 更新日志

本文件记录各版本的玩家可见改动。发布时 CI 会取出对应版本的段落，作为 GitHub Release 说明、Modrinth 与 CurseForge 的更新日志。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；版本号遵循语义化版本。

## [未发布]

### 新增

- **新增「ME样板磁盘管理终端」**（电缆上挂载：`ae2_pattern_disk:pattern_disk_management_terminal`）：与编码终端同一套磁盘逻辑与编码区，但磁盘列表换成一张铺满面板的表格——按持有磁盘的机器分组（组头带图标、名字与张数，以及该组的显示/隐藏开关），一张盘占一行：首格是磁盘本身（底色叠一层浅绿，纯上色不带贴图），后面 16 格是盘内样板（盘内容按需从服务端拉，视口变了才重发，另有一秒心跳兜底）——样板格显示的是该**主产物**，与样板访问终端同一口径（AE2 的 `EncodedPatternItem#getOutput`），任何类型都直接显示（非物品产出由 AE2 包成伪物品），不必按住 Shift 才看得出它做什事；解不出来的样板仍然标红。搜索、左键写盘、右键写标记、Shift+右键覆写标记、中键改名全部沿用编码终端的口径。组件贴图暂用编码终端那套，界面贴图用自绘的 `guis/pattern_disk_management_terminal`；面板 340×220，与贴图内容一致。

### 修复

- **管理终端表格的落点与底色按贴图重新对齐**：表格是自绘的，原先把填充区起点当成面板的 (1,1)、标题条按 17px 算，而行条切片又从标题条末行起手——三处合起来让画出的物品、磁盘格浅绿底色与鼠标命中都偏了约 8px，行条还丢了 y=35 那条 1px 分隔线。现在按贴图实测取值：填充区从 x=8 起（落点口径沿用 AE2 的「格子 x/y 即物品左上角」，底图在它 −1 处，所以取 `LIST_X=7`）、标题条 18px、行条切片 y18..35；绘制与命中仍共用同一组常量。
- **修掉「ME样板磁盘管理终端」开出编码终端界面**：菜单实例携带的 `MenuType` 才会被服务端写进开界面的包、由客户端据此挑屏幕，而编码终端菜单的构造器写死了自己那一个，管理终端菜单沿用父类构造器时被一并带成编码终端类型——菜单数据其实是管理终端的，所以不崩、只是开错界面。现在类型改由构造器参数下发，管理终端传自己的类型；编码终端与 ExtendedAE Plus 适配子类行为不变。同一错位还静默废掉了按管理终端类型登记的 JEI/EMI 配方处理器（客户端报的是编码类型，永远匹配不上），一并修好。
- **管理终端不提供上传入口**：它走标记路线，不把样板上传到网络。NEO ECO 的上传按钮原本由父屏 `init()` 主动注入、继承后照样会显示，现由屏幕钩子挡掉（默认保留，编码终端与 EAE+ 适配屏不受影响）；ExtendedAE Plus 那条本就挂在编码类型下，天然不会出现在管理终端。

- **修掉 0.4.0 引入的可选依赖硬引用**：编码终端的菜单/界面适配子类被「总是加载」的类直接引用，而类一被加载就会解析它 `implements` 的 ExtendedAE Plus 接口——`if` 守卫只挡执行、不挡加载，于是没装 EAE+（或装着不含该契约的版本，包括在架的 1.6.2）的客户端会在启动注册阶段直接崩在 `NoClassDefFoundError: com/extendedae_plus/api/upload/IPatternUploadMenu`。现在两个适配子类只经反射按类名创建（`ExtendedAEPlusCompat` / `ClientExtendedAEPlusCompat`），总是加载的类里不再出现它们的名字。顺带删掉 `mods.toml` 里那条凭空生出的 EAE+ 依赖声明：代码对它的版本零要求，真正要认的是「那两个接口类在不在」。契约探测同时检查上传菜单与上传终端两个接口，只带其中一个的构建会整体退回基类，不会出现「一半能用」的状态。

## [0.4.0] - 2026-09-20

### 新增

- **手里拿着工作方块右键磁盘，标记现在按「那个方块跑哪类配方」来写**。此前标记只能来自「刚导入过配方」这个会话状态，切一次模式或重开界面就没了，于是拿工作方块右键只能退回模式标记（显示成「合成」/「处理样板」），写下的标记与光标上那个方块无关。现在终端会反查光标上那件工作方块对应的配方类别（JEI 在场时按它自己的类别，EMI 在场时借 EMI 的类别名），原版工作方块另有兜底：工作台→合成、熔炉→烧炼、高炉→高炉烧炼、烟熏炉→烟熏、营火/灵魂营火→营火烹饪、切石机→切石、锻造台→锻造。织布机、制箭台、制图台以及铁砧/磨石/堆肥桶在 JEI 与 EMI 里都没有对应的配方类别，拿它们右键不会写入标记（会照常当场提示）。
- **编码终端新增「精确设置过滤槽数量」子屏**：处理模式的过滤槽可以按数值设置，外观与交互沿用 AE2 样板编码终端同款对话框。
- **从 JEI 配方页点「编写样板」转移配方时，标记记的是那个配方页的类别**。JEI 用适配器包装通用处理器时会丢掉上下文里的类别，现在类别由配方传输监听器在传输开始前记下、通用处理器取用，传输结束即清（否则会串到下一条标记）。
- **编码终端接上 ExtendedAE Plus 的「上传到供应器」按钮**：本终端现在实现 EAE+ 自己的终端上传契约（`IPatternUploadMenu` / `IPatternUploadTerminal`），按钮由 EAE+ 注入、摆到 NEO ECO 上传按钮的正下方。该契约目前只存在于 EAE+ 的 1.21.1 分支（发布版还没有这两个类），所以用类探测而不是版本号判断：没带契约的构建照常加载，只是没有这个按钮。

### 变更

- **可选依赖的版本下限写进了模组元数据**：JEI 若在场需 ≥19.56.0、EMI ≥1.1.22、ExtendedAE Plus ≥1.6.2、Jade ≥15.10.6，AE2 本体仍需 ≥19.0.0；NEO ECO、AE2 JEI 集成、AE2 Crystal Science 不设自己的下限。
- **无标记磁盘开关的口径改为「一个搜索条件只筛它自己那一维」**：此前关闭开关时，即使搜索栏空着也会把无标记的空盘剔出列表，与开关自己的说明（「常态显示无标记样板磁盘」）相反。现在：无搜索条件 → 全部列出；名字搜索 → 只比名字（无标记的盘照常参与）；`#` 标记搜索 → 只比标记（无标记的盘没东西可匹配，自然不出现）；开关打开 → 无标记的盘不受搜索过滤、一律留下。副作用：搜索栏筛完只剩一张盘时「编写样板」会直接写进去，现在那张盘也可能是无标记盘。
- **普通右键不再把磁盘标记冲成当前模式标记**：手里没有可识别的工作方块、也没有刚导入的配方类别时，这次右键不写入，改在聊天栏说明（提示与回执和上传链路一样由服务端发言）；要刻意写模式标记请用 Shift+右键 + 搜索栏填 `#mode:<crafting|stonecutting|smithing_table|processing>`。工作方块的判定口径也定下来了：**只认拿在鼠标光标上的那一件**，主手/副手不参与——终端里整理磁盘时工作方块通常正是这么被拿起来的，此前只认手会报「手里没有可识别的工作方块」。

### 修复

- **JEI 下熔炉/烟熏炉会被认成燃料类别**：反查按机器自己的配方类别判定，不再落到燃料这一档。
- **ExtendedAE Plus 的发布版与 JEI 19.56 不兼容，客户端进世界即崩**：它的 `BookmarkOverlayMixin` 还在引用被 JEI 19.56 搬走的 `CombinedInputHandler`，JEI 一启动就 `ClassNotFoundException`。EAE+ 侧已修但尚未发版，所以开发运行期与打包依赖口径都改用它的本地构建产物（对应 CI 变量 `EAEPLUS_JAR_URL`）。

### 文档 / 构建

- **项目页正文独立**：Modrinth 正文移到 `PLATFORM.md`，`README.md` 回归开发者文档，另加 `PLATFORM_MCMOD.md` 作为 MCMOD 项目页正文。
- **`release.yml` 增加第三个本地专属依赖**：EAE+ 的终端兼容层接口（`IPatternUploadMenu` / `IPatternUploadTerminal`）目前只存在于它的 1.21.1 分支，Modrinth 最新发布版还没有这两个类，因此编译期与开发运行期都用 `libs/` 里的构建产物，CI 侧由仓库变量下载并校验哈希。
- **开发运行期默认改用 JEI**（`./gradlew runClient -Pemi` 切回 EMI）：EMI 仍保留编译期兼容，但 JEI 那边的兼容得真跑起来才能验，AE2 的 JEI 桥接模组（AE2 本体不带，其元数据又把 JEI 标成必需）也跟着 JEI 分支走。

## [0.3.2] - 2026-09-19

### 文档

- **内置指南补上 0.3.0 的功能，并开始提供英文版**。此前指南只有中文、写在默认位置，内容停在 09-14，例如还写着「潜行左键磁盘→绑定类型并改名」，而该行为早已被推翻。现在中文移到 `_zh_cn/`、默认位置放英文（GuideME 约定：缺译文时回退到默认页）。新写进去的有：磁盘**标记**（右键记当前配方类别、Shift+右键记搜索栏文本，标记只显示在编码终端的磁盘列表里、不改磁盘名）、终端磁盘列表的筛选与 `#` 标记搜索、无标记磁盘开关、写盘回执与编写样板时自动落盘、空白样板取自网络、切石三行候选，以及 NEO ECO 上传按钮。指南页在导航里的序号也提到 1000 段，不再插到 AE2 自身内容前面。
- **README** 补上「ME 样板磁盘编码终端」一节与标记说明，并写明 ExtendedAE Plus 与 NEO ECO 的接入行为。此前 README 里根本没有编码终端的功能节。
- **项目描述更新**：旧描述只提了样板磁盘、供应器和转存器，没提两台装配室与编码终端。
- **新增 `sync-project-pages.yml`**：项目描述变化时自动同步到 Modrinth（短描述取自 `gradle.properties` 的 `mod_description`，正文取自 README 并掐掉构建说明）。独立于发版流程——它需要 PAT 带 PROJECT_WRITE，scope 一缺失就会失败，放进发版里会让「版本已发布、job 却变红」。CurseForge 没有可写项目描述的公开接口（只有只读的 GET /v1/mods/{id}/description），那边仍需人工维护。

## [0.3.1] - 2026-09-19

### 修复

- **修复了因 neoecoae 硬依赖泄漏导致的启动崩溃**。`PatternDiskEncodingTermMenu` 此前 `implements` neoecoae 的接口 `PatternEncodingTermMenuExtension`，而 neoecoae 是可选依赖——未装 neoecoae 的玩家加载该菜单类时会因接口类不存在而抛 `NoClassDefFoundError`。现在所有 neoecoae 硬引用已收敛到 `integration/neoecoae/` 包内，通过「mod 存在检查 + 独立 holder 类」的条件加载模式按需接入；menu 与 screen 的字节码里不再出现第三方符号（艾琳审查已通过）。

## [0.3.0] - 2026-09-19

这一版把「样板磁盘编码终端」补成了一台能用的机器：磁盘列表能按标记归类与搜索、写盘失败会说明原因、编好的样板在目标唯一时直接落盘。

### 新增

- **样板磁盘标记**。磁盘可以记一个标记，说明它属于哪台机器 / 哪类配方；标记在磁盘 tooltip 里单独一行显示，不再改写磁盘名。导入配方后标记取该配方在 EMI 里的类别（熔炉、烟熏、切石机……各是各的），手动编码时退回编码模式。
- **磁盘列表搜索栏**。按磁盘名搜索；以 `#` 开头则按标记搜索，模式标记会归一到对应类别的名字，所以「导入过配方」和「手动编码」的同机器磁盘叫同一个名字、互相搜得到。
- **显示无标记磁盘的开关**，位于搜索栏右侧。关闭时只列有标记的磁盘（无标记的盘不属于任何配方类型）；打开时把无标记的盘也列出来，且它们不受 `#` 标记搜索影响——这样才看得到、够得着那些还没标记的新盘。
- **鼠标操作**：左键把当前编码的样板写入该磁盘；工作方块右键用当前配方类别覆写标记；Shift+右键用搜索栏里的文本写标记，搜索栏为空则清除标记；工作方块中键按机器名重命名。
- **编写样板时自动落盘**。搜索栏筛完恰好剩一张磁盘时，点「编写样板」直接把样板写进去，省掉「编出一个样板再点磁盘」两步。
- **写盘结果一律有回执**，且在聊天栏（按 T 可查）。成功会说写到了哪张盘；失败会说原因：盘已满、已锁定为其他类型的样板、盘里已有产出相同的配方、这份样板解不出类型、目标磁盘已不在列表里、没有恰好一张盘可自动写入。
- **空白样板槽改为网络镜像**。样板直接从 ME 网络取，不再需要往槽里放空白样板；存盘后回退的空白样板按「网络 → 背包 → 编码槽」的顺序落位。
- **切石模式的配方选项改为三行**（原两行），行间不留空隙，一次能看到 12 个选项。
- **对外 API**：导出 `PatternDiskApi` 门面，外部模组可接入样板磁盘；样板磁盘供应器接受 ExtendedAE Plus 的上传写入。

### 修复

- **唯一匹配时却不写盘、也不给任何提示**。磁盘的 `serial` 从 `Long.MIN_VALUE` 起自增、恒为负数，而自动写盘这条链路拿 `-1` 当「没有目标」并按符号判断，于是客户端判定恒真、服务端门限恒假：既永远不写盘，又每次都提示「不是恰好一张」。改为按张数判断。
- **标记粒度退化成只有编码模式**。导入配方时记下的类别会被随后的 `setMode`（导入内部会切模式，而手动切模式要丢弃旧类别）当场清掉，导致绑定写下的始终是模式标记，分不出具体配方类别。调整了设置顺序。
- **切石会出现「切石」和「切石样板」两种标记**。导入态用配方类别、手动态用编码模式，两套词；现在模式标记按对应类别的名字显示，两套说法收敛成一个。
- 磁盘写入后列表刷新滞后（改为写入即推送，不再等指纹比对）。
- 中键改名读到的是旧标记（改为等权威列表到货后再执行）。
- 批处理装配室的 PAT 分组返回真实分组而非 `nothing()`。

### 变更

- 样板磁盘槽的 tooltip 措辞统一：手势用词一致、宾语一律称「该样板磁盘」，并精简掉冗长的规则说明行。
- 搜索栏 tooltip 走 AE2 原生链路。

## [0.2.3] - 2026-09-18

- 批处理装配室：自适应供料窗口、缓存栏闲置退回与索要键优先返回。
- 编码终端配方输入槽补可合成标记与角标提示。

## [0.2.2] - 2026-09-17

- 移除开发环境专用依赖（集成动力 / 精致存储 / EnderIO 系列）。
- 补 Jade 的批处理装配室配置翻译。

## [0.2.1] - 2026-09-17

- 首个版本：样板磁盘按类型存放已编码样板，配套样板磁盘供应器与样板转移器。
